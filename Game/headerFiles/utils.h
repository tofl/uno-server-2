#include <string>
#include <stdlib.h>

#ifndef SOCKETS_WITH_THREAD_UTILS_H
#define SOCKETS_WITH_THREAD_UTILS_H

#endif //SOCKETS_WITH_THREAD_UTILS_H

namespace utils {
    std::string formatCardsToUserResponse(std::vector<std::string>);
}